
<%= simple_form_for(@workstation, html: {class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <div class="form-actions">
   <%= f.button :submit, :class => "btn btn-primary" %>
   <%= link_to "Cancel", jobs_path, :class => "btn" %>
   <div>
<% end %>